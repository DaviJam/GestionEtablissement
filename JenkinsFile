// Declarative //
pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				bat 'cd GestionEtablissement && mvn clean install'
			}
		}
		stage('Test') {
			steps {
				bat 'cd GestionEtablissement && mvn test'
			}
		}
		stage('Deploy') {
			steps {
				echo 'Deploying....'
			}
		}
	}
}